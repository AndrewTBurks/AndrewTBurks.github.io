import React, { Component } from 'react';

class Project extends Component {
  state = {}
  render() {
    let { name, abstract, team, imgref, toolref, reporef, pdfref } = this.props;

    return ( 
      <div>
        name
      </div>
     )
  }
}
 
export default Project;